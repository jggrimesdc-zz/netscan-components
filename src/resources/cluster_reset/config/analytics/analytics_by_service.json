{
  "columnDefinitions": [
    "SERVICE text",
    "TIMESTAMP timestamp",
    "SCAN_USER_ID text",
    "DUNS text",
    "IP_ADDRESS text",
    "DOMAINS text",
    "PORT text"
  ],
  "name": "by_service",
  "primaryKey": [
    "SERVICE",
    "TIMESTAMP"
  ],
  "tableOptions": "CLUSTERING ORDER BY (TIMESTAMP DESC) AND compaction = { 'class' : 'LeveledCompactionStrategy' }"
}
